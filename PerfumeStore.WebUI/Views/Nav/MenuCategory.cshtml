@model IEnumerable<string>

<h3 style="text-align:center"><u>Category</u></h3>

@foreach (var link in Model)
{
    @Html.RouteLink(link, new
    {
        controller = "Perfume",
        action = "List",
        category = link,
        page = 1
    }, new
    {
        @class = "btn btn-block btn-default btn-lg"
            + (link == ViewBag.SelectedCategory ? " btn-primary" : "")
    })
}